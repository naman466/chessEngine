/*
initialization file to include the initial state in the engine.cpp
*/

#include "board.h"

int map120to64[64];
int map64to120[NUM_SQUARES];

void Init120to64(){

    int index = 0;
    int file = static_cast<int>(Files::A_FILE);
    int rank = static_cast<int>(Ranks::RANK_1);
}

void AllInit(){

}